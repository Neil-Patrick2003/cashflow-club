/** A local chapter of the club, administered from the configuration page. */
export type Chapter = {
    id: number;
    name: string;
    city: string;
    is_active: boolean;
    created_at: string;
    updated_at: string;
};
